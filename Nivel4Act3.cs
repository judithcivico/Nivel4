using System;

public class StringBasic() {
	static void Main(string[] args) {
		private String cadena = "";
		int i, contadorVoc, contadorCons;
		contadorVoc = 0;
		contadorCons = 0;

	}
}
	public Constructor(String cadena) {
		this.cadena = cadena;
	
	private String cadena {
		get { return cadena; }
		set { cadena = value; }
	}
	}
private vocalesConsonantes() {
	public static void Main() {
		
		Console.WriteLine("Escribe una frase: ");
        string cadena = Console.ReadLine().ToLower();

        for (int i = 0; i < cadena.Length; i++) {
        if (cadena[i]  == 'a' || cadena[i] == 'e' || cadena[i] == 'i' || cadena[i] == 'o' || cadena[i] == 'u') {
            contadorVoc++;
        } else if((cadena[i] >= 'a' && cadena[i] <= 'z') || (cadena[i] >= 'A' &a&; cadena[i] <= 'Z')) {
            contadorCons++;
		}
        Console.WriteLine("El total de vocales es: ", contadorVoc);
		Console.WriteLine("El total de consonantes es: ", contadorCons);

        Console.ReadLine();
    }
private consonantes()
}